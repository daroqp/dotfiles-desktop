### Empezar una nueva session con un nombre: 

``` bash
tmux new -s mysession
```

###  Mostrar todas las session

``` bash
tmux ls
```

### Abrir la session con el nombre que le puse

```bash
tmux attach-session mysession
```

